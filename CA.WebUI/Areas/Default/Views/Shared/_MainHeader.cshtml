@using CA.Resources
<nav class="navbar navbar-dark bg-dark navbar-expand-lg fixed-top">
    <a class="navbar-brand" href="@Url.Action("Index", "Home")">ChatApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="@Url.Action("Index", "Home")">@HomeResources.Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index", "User")">@HomeResources.Users</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Index", "Chat")">@ChatResources.Chat</a>
            </li>
        </ul>
        
        <ul class="my-2 my-lg-0 list-inline navbar-nav">
            @Html.Action("UserLogin", "Home")
        </ul>
    </div>
</nav>